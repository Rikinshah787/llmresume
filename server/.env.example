PORT=4000
CORS_ORIGIN=http://localhost:3000

# GRO API (external AI-editing service)
GRO_API_BASE=https://api.gro.example.com
GRO_API_KEY=your_gro_api_key_here
GRO_MODEL=gpt-resume-1

# When true, the server will use a local mock proposer instead of calling Gro API.
# Useful for development and testing when you don't have GRO credentials.
GRO_MOCK=true

NODE_ENV=development
